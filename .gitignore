# Hypr monitors symlink (host-specific, managed at runtime)
hypr/.config/hypr/config/monitors.conf
hypr/.config/hypr/old/config/monitors.conf
# Secrets directory
.local_secrets/
home/.gitconfig
nushell/.config/nushell/history.txt

# Nix build results
result
result-*
# Agent worktrees, which `git add -A` would otherwise offer as an embedded repository.
/.claude/worktrees
